import * as snarkjs from "snarkjs";


describe("Merkle tree insertion", () => {
  it("should create a proof when inserting a new leaf to an empty tree", async () => {});

  it("should create a proof when inserting a new leaf to a non-empty tree", async () => {});

  it("shoud fail to create a proof when inserting a new leaf to a non-empty tree with invalid parameters", async () => {});
});

describe("Merkle tree deletion", () => {
  it("should fail to create a proof when deleting a leaf from an empty tree", async () => {});
});

describe("Merkle tree update", () => {
  it("should create a proof when updating a leaf in a non-empty tree", async () => {});
});

describe("Merkle tree membership proof", () => {
  it("should create a proof when proving membership of a leaf in a non-empty tree", async () => {});
});
